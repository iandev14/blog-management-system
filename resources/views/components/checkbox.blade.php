<div class="flex items-center">
    <input type="checkbox" id="{{ $id }}" name="{{ $name }}" class="form-checkbox text-blue-600">
    <label for="{{ $id }}" class="ml-2 text-sm text-gray-700">{{ $label }}</label>
</div>